{% extends 'base.html.twig' %}

{% block title %}Hello VilleController!{% endblock %}

{% block body %}
<div class="new_town">
    <h1>Créer un lieu</h1>
    <div class="new_town_content">
    {{ form_start(form) }}
    {{ form_row(form.nom) }}
    {{ form_row(form.codePostal) }}
    {{ form_row(form.enregistrer) }}
    {{ form_end(form) }}
    </div>
</div>
{% endblock %}
